---
"@texoport/effect-parser-combinators": minor
---

Add bulk token, delimiter, lookahead, and list parser combinators with streaming support. Invalid parser configuration now fails through the Effect error channel instead of throwing.
